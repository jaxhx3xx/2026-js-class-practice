/**
 *
 * @param {*} productPrice
 * @returns
 */
function taxAmount(productPrice) {
  const tax = 0.1;
  return productPrice * tax;
}
/**
 *
 * @returns
 */
function getTop5() {
  return ["정재희", 2, 3, 4, 5];
}
